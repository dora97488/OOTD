# 中央氣象署(CWA) 開放資料 API 授權碼。
# 取得：https://opendata.cwa.gov.tw → 註冊/登入會員 → 「取得授權碼」（格式 CWA-xxxxxxxx-xxxx-xxxx）。
# 用法：cp .env.local.example .env.local 後填入下行；重啟 npm run dev。
# .env.local 已被 .gitignore（*.local）排除，不會進版控。
VITE_CWA_API_KEY=CWA-XXXXXXXX-XXXX-XXXX
